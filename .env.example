GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-lite
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_BUCKET=uploads

# Opcional. Vacío = permite localhost/127.0.0.1 con cualquier puerto (desarrollo).
# Si lo rellenas, usa el origen exacto del navegador (puede ser http://127.0.0.1:5173 en vez de localhost).
CORS_ORIGINS=
